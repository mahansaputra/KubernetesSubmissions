apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project-app-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: project-app-service
                port:
                  number: 80